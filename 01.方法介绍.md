# 方法介绍
```python
1. 初始化方法
ldap.initialize(uri[, trace_level=0[, trace_file=sys.stdout[, trace_stack_limit=None[,
bytes_mode=None[, bytes_strictness=None[, fileno=None]]]]]]) → LDAPObject
object

2. 全局配置设置 
ldap.get_option(option) → int|string
    此功能返回选项指定的全球选项的值
ldap.set_option(option, invalue) → None
    此功能设置由选项指定的全球选项的值。
    

    
    
    
    
    
    
```